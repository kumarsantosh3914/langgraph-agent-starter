[project]
name = "langgraph-starter"
version = "0.1.0"
description = "LangGraph starter notebook"
requires-python = ">=3.12"
dependencies = [
    "langgraph>=0.2.0",
    "ipykernel>=6.29.0",
    "langchain-openai>=1.2.1",
    "python-dotenv>=1.2.2",
]

[tool.uv]
package = false
